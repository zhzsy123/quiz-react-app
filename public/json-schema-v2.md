# JSON Schema v2 规范草案

请优先查看仓库中的 `docs/json-schema-v2.md` 获取完整说明。

本站推荐工作流：

1. 将 PDF / DOCX 试卷和本规范一起交给 AI
2. 让 AI 输出符合 `schema_version = "2.0"` 的 JSON
3. 将 JSON 导入本站

建议同时下载：

- `sample-schema-v2.json`
- `sample-schema-v1.json`
